// ==============================================

exports.seed = function (knex) {
  
  // --------------------------------------------
  
  const sigs = [
    {
      email: 'TESTjosh@josh.com',
      first_name: 'josh',
      last_name: 'holloway',
    }
  ];

  // --------------------------------------------

  return knex('sigs').insert(sigs);

  // --------------------------------------------
};

// ==============================================